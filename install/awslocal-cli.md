## LocalStack AWS CLI

**Dependencies:**

- Python

Run:

```bash
pip install awscli-local
```

If after installing, the `awslocal` command is still not found, ensure that
`~/.local/bin` is in your `PATH` variable.
